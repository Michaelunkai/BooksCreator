# Folio verification

Recorded 9 September 2026. Implementation is running locally at http://127.0.0.1:3001.

**123 automated tests passed in nine files. Production build and TypeScript passed. Folio auto-starts the known local Qwen3.8 runtime when its installed model files are present, auto-detects the strongest completion-capable loopback model, then falls back through the bundled Qwen3.5 on-device writer and lazy Janus 1.3B browser art route with deterministic offline fallbacks. The current live server reports `localModelAvailable:true`, `localModelName:"Qwen3.8 27B XXS"` and `localModelContext:8192`; a fresh no-key summary returned `source:"local-model"`. WebGPU generation and browser visual acceptance remain unverified because approved Chrome blocks the loopback page.**

## Acceptance evidence

| Criterion                           | Status and evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ----------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Comfortable book writing            | Implemented: Tiptap rich editor, chapters, fonts, formatting, undo/redo, find, focus mode. Six `tests/app.test.tsx` integration tests exercise the actual React components and Tiptap in JSDOM, including chapter switching and reopening. Real-browser appearance/input still pending.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Dark mode by default                | Implemented: Folio opens with a dark canvas, dark panels and controls, and a readable dark manuscript surface. The built root HTML sets the dark theme color; rendered browser contrast and mobile appearance still await the approved visual session.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Reliable save and reopen            | Verified with real HTTP and disk writes. `tests/runtime.test.ts` stops and restarts separate Node processes, reloads the exact workspace and verifies illustration bytes. `tests/workspace.test.tsx` covers debouncing, serialized updates, StrictMode, failed saves, lost acknowledgments and retained edits.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Conflict and recovery protection    | Verified in 13 workspace tests: separate recovery identities for duplicated tabs, non-destructive reload migration even when storage is full, closed-tab recovery discovery, revision conflicts, preserving server and pending edits as separate books. Backend refuses corrupt or missing-primary replacement and preserves the previous file.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Idea to chosen lines/pages          | Implemented: words, estimated lines at 12 words and pages at 250 words. Writing supports up to 100,000 target words; hosted requests use up to 5,000-word sections, automatic local passes size themselves from the detected context window, and the bundled browser route uses conservative 4,096-token request sections so long drafts can continue safely. Backend payload/length tests and component tests verify single and two-section requests, continuity context, context-window fitting with reserved output headroom, partial/error/cancel preservation and no automatic retries. If the automatic local runtime disappears during a long draft, the companion recalculates the remaining sections and safely hands the draft to the browser writer; a regression test covers the 600/400-word handoff. A full 100,000-word live run has not been performed. |
| No-setup writing and summaries      | Implemented and tested: when `/api/settings` has no hosted provider, the server probes common loopback OpenAI-compatible runtimes in parallel and selects the strongest completion-capable model; the launcher starts the known Ubuntu Qwen3.8 runtime when its model files exist, while an absent runtime leaves browser/offline fallbacks available. On the current host `/api/settings` reports `configured:false`, `localModelAvailable:true`, `localModelName:"Qwen3.8 27B XXS"` and `localModelContext:8192`; live no-key `/api/generate` returned a clean `source:"local-model"` summary, while `/api/illustrate` returned the expected 428 hosted-image handoff explaining that the free browser art model and offline art study are ready. If a runtime stops after the probe, the companion falls through to the browser model and deterministic writer without stranding the draft. Write, refine and summary modes remain review-first; summaries support idea, book, chapter and passage scope, open at a few sentences, preselect an available passage, and offer one sentence, a few sentences, paragraph and page targets. Idea-only summaries require a nonblank idea and never substitute chapter or book context. `tests/companion.test.tsx`, `tests/local-writer.test.ts`, `tests/browser-writer.test.ts` and the automatic loopback selection tests in `tests/server.test.ts` cover the routes. |
| Creative literary voice             | Implemented prompt controls: original idea, author sample, voice notes, book language/genre, POV, tense, selected passage, chapter text, chapter map and story bible. The bundled Qwen3.5 prompt preserves established facts and asks for specific, varied prose without filler; the deterministic fallback now carries the submitted premise into its opening and varies later paragraph movement instead of repeating one template. Literary quality still needs human/browser assessment; authorship judgments cannot be guaranteed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Bundled free model                  | Shipped and statically verified: Qwen3.5 9B q4f16 WebLLM assets (135 files, 4.71 GiB) plus bundled 4B (86 files, 2.23 GiB) and 2B (39 files, 1.01 GiB) profiles, each with a matching WebGPU runtime and integrity manifest. The no-setup order is automatic loopback model first when present, then the higher-precision q4f32 9B WebLLM record, the shipped q4f16 profile and local smaller profiles, then the deterministic writer if the device or network cannot load a model.                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Review and safe insertion           | Tested preview editing, changed-selection protection and scoped previews. Delayed image paste tracks its original insertion point through edits, preserves later text/selection/focus, and cancels when the chapter closes. App insertion preserves a prior revision; replacement verifies chapter and range text. Actual browser text-selection and insertion remain pending.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Story-grounded artwork              | Implemented excerpt, bounded chapter context, prioritized character/plot/place/world/story-note cues, style/size/direction prompts, a free Janus 1.3B browser generation route when WebGPU is available, deterministic text-grounded local art fallback whose visual seed and structural motifs follow the same story material, original generated sample illustration, image gallery, captions, upload/paste, download and insertion. Mock provider tests verify hosted image handling; browser model execution still needs the approved live WebGPU check.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Art survives saving                 | Verified with the actual generated 3,332,383-byte PNG through the upload API, disk save, server restart and byte-for-byte retrieval.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Revisions                           | Tested save/restore of named snapshots, preservation before restore and continued saving after 101 revisions. UI supports deleting unwanted versions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Backup/import/export                | Thirteen tests verify safe portable JSON including gallery/current/archived artwork, fresh identifiers, imported validation and sanitization; Word ZIP contents, EPUB XML/navigation/order/art, HTML, Markdown and text. Reading exports include inserted artwork only, with no duplicated captions. Markdown metadata/prose metacharacters are escaped, top-level DOCX text is preserved, RTL DOCX title-page and manuscript paragraphs carry bidi metadata, and failed multi-image imports clean up earlier uploads. |
| PDF                                 | Implemented via browser print dialog with bounded image waiting, a timeout, and iframe cleanup on print errors. Actual print-dialog interaction and final PDF rendering are unverified because approved browser control is blocked.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Hebrew/RTL                          | Unicode counting, right-to-left metadata and text preservation are tested, including real disk/process restart. Shell, settings, companion, story bible and illustration surfaces now inherit the book direction and language; DOCX title, subtitle, author and manuscript paragraphs carry bidi metadata. Real browser RTL layout and Word/EPUB reader presentation are unverified. |
| Provider security and errors        | 47 backend tests: loopback Host/Origin protection, payload validation, safe media paths, key secrecy and origin binding, loopback-only automatic discovery, embedding-only model rejection, context-window fitting, blank idea-only rejection, missing-key behavior, automatic strongest loopback model selection, 401/403/404/429/500 messages, timeout/disconnect cancellation, incomplete/malformed responses and no fallback prose. Hosted generation remains opt-in; the 428 response explains that the on-device writer, browser art model and offline art study are ready and a provider is only for hosted generation. Strict Astra mock rejects unsupported fields; request omits temperature/top_p/top_logprobs/logprobs and uses low reasoning. |
| Responsive design and accessibility | Implemented responsive chapter/companion drawers with dialog semantics, focus transfer/restoration and keyboard trapping, readable controls, live save status, semantic labels, RTL propagation, ARIA tabs, keyboard shortcuts, modal focus management and reduced-motion styles. Desktop/mobile screenshots, real keyboard traversal and assistive-technology checks remain pending.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

## Latest checks

- `npm test`: **123 passed**, nine files, 4.21 seconds in the latest Vitest run. Breakdown: backend 47; exports 13; companion/art 24; browser writer 5; browser illustrator 3; local writer 10; App/editor 6; workspace 13; process runtime 2.
- `npm run build`: TypeScript passed and Vite production build passed (51.99 seconds in the latest build; current app bundle `/assets/index-CIwLZOFH.js` with `/assets/index-svv1LWK_.css`). The output keeps the static model bundle in place when a browser has a shard open, and includes a lazy Janus worker plus its WebGPU runtime, so routine rebuilds remain safe. DOCX/ZIP are loaded on demand. Vite reports a bundle-size advisory for the rich editor and harmless third-party PURE annotation notices; these were not hidden.
- `Start-Folio.ps1`: Windows PowerShell parsing passed; the guarded launcher compares `src`, `server`, `public`, and the production entry/config files with `dist/index.html`, rebuilds stale production output automatically, records the confirmed server PID, restarts that server when backend source is newer, and printed the ready message. The missing-model path is non-fatal and retains browser/offline writing; the current host also has the known local Qwen runtime available. The `Start Folio.cmd` child process returned exit code 0.
- Latest actual-process test used four distinct processes: 15544, 32540, 7464, 40216. It preserved an RTL manuscript, one chapter snapshot, two story notes, one full-size illustration and two workspace revisions. The corrupt-primary process refused reads/saves with 503; after shutdown and restoration of the previous file in the guarded test directory, another fresh process loaded revision 1 and saved again.
- Uploaded image SHA-256: `32bd0fe354f5f361ddf18bccf6d22fe03c54460d4ec470c129ac2f7cfff968ec`.
- Running local app: `/api/health` reports `folio-writing-studio`, `ready`; root HTML returns 200 with the dark theme metadata and current bundle `/assets/index-CIwLZOFH.js` plus `/assets/index-svv1LWK_.css`; the served CSS keeps the illustration preview surface, empty art frame and status states dark, and the served JS contains the visible “Free writing, summaries and art are ready without setup” banner. On the current host `/api/settings` reports `configured:false`, `localModelAvailable:true`, `localModelName:"Qwen3.8 27B XXS"` and `localModelContext:8192`; the live `/v1/models` record is `/root/models/Qwen3.8-27B-UD-IQ3_XXS.gguf`. Live no-key `/api/generate` returned the expected `source:"local-model"` summary; `/api/illustrate` returned the expected 428 handoff explaining that the free browser art model and offline art study are ready. The sample workspace reopens at revision 1 with one book, three chapters, one sample illustration and five story notes. All three bundled writing-model manifests and the Janus manifest return 200 from the same production server. No browser was controlled to produce this HTTP check.
- The fresh current-host live summary used the idea-only route and returned 30 words against a 28-word target. This proves the running local route and its provider selection, not universal model availability, exact target-word compliance, browser WebGPU execution or human literary quality.
- Full test output: `.data/test-results.log`; typecheck output: `.data/typecheck.log`; build output: `.data/build.log`. Test data is created in guarded temporary directories and cleaned up; the user's `.data` library is not used by the process tests.

## Browser acceptance blocker

Fresh `agent.browsers.list()` discovery found the approved Chrome extension entry on Profile 2 / Person 1, and a new visible Folio tab was claimed from the fresh `user.openTabs()` result. Chrome then blocked both `http://127.0.0.1:3001/` and `http://localhost:3001/` with `ERR_BLOCKED_BY_CLIENT` before the app could render. Browser control therefore failed closed; no substitute browser, profile, in-app browser, or physical pointer control was used. JSDOM tests are component tests, not browser screenshots or user-route proof.

## Design comparison ledger

Primary concept: `design/folio-concept.png`, 1536 × 1024, generated before implementation and inspected with `view_image`. Sample art: `public/assets/tide-illustration.png`; its built-in generation prompt is saved in `design/illustration-prompt.md`.

There is **no implementation browser screenshot** and no native-size or mobile viewport verification, so visual fidelity cannot be signed off. The following source-level choices are prepared for the later comparison:

| Comparison point           | Source implementation / intentional deviation                                                                                                                                                                            | Render verification                         |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------- |
| Layout                     | Top bar, chapter/book rail, central manuscript paper, right writing companion; narrower panels and responsive drawers adapt to viewport.                                                                                 | Pending                                     |
| Typography                 | Bundled EB Garamond for manuscript/literary headings, DM Sans for controls; user-selectable editor font and size.                                                                                                        | Pending                                     |
| Palette                    | Dark forest canvas #0c1212, layered panels #121918, dark manuscript #18201f, mint accent #86c9aa, muted gold and fine borders.                                                                                           | Pending                                     |
| Copy                       | Primary concept headings/nav/chapter names retained; functional save errors, generation options, progress and recovery actions added. Counts are real; example prose is original and labeled as a sample in the library. | Pending above-the-fold rendered diff        |
| Artwork                    | A separately generated coastal engraving replaces the concept's tiny cover and shoreline decoration; it is a real reusable sample/gallery asset.                                                                         | Pending asset framing comparison            |
| Controls and icons         | Code-native forms and Tiptap tools, thin Lucide icon family, review/insert/cancel, export and provider dialogs.                                                                                                          | Pending pointer, alignment and focus checks |
| Spacing and responsiveness | Central paper has generous margins; source breakpoints at 1600/1200/1000/720 px and artwork-specific breakpoints.                                                                                                        | Pending clipping/overflow checks            |

## Required final live checks

1. Resolve the Chrome loopback blocking and render Folio in the approved Profile 2 / Person 1 browser, then claim the exact Folio tab and complete the visible user-route checks.
2. Create a book and two chapters; type and format text using the approved visible synthetic cursor route. Verify save acknowledgment, reload and fresh server reopen.
3. Add story notes and voice preferences, use Hebrew/RTL, save a named version, change it and restore it. Inspect desktop and narrow viewport layouts.
4. Exercise the no-setup route: create a draft, summarize an idea, chapter or passage, create a local art study, review/edit each result and insert only the approved output. A provider remains optional.
5. If a provider is intentionally supplied inside **Connection settings**, generate a small hosted passage and illustration, inspect quality/continuity, edit and insert the review. Do not send credentials in chat. No provider is currently configured and no unrelated credentials were accessed.
6. Export/import a complete illustrated backup, open Word/EPUB outputs in their readers, and verify the PDF print route.
7. Capture the implementation screenshot, compare it directly to the concept via `view_image`, and finish the responsive/focus/copy/asset fidelity review.

Implementation is reviewable and the writing app is available now. The full goal should not be reported as completely verified until these remaining external prerequisites are satisfied.



